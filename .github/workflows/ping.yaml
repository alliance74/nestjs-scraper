name: Render Server Pinger

on:
  schedule:
    - cron: "* * * * *"   # every  minute
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render app
        run: |
          curl -s https://events-deals-api.onrender.com/
